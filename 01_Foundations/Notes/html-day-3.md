Knowledge Check Answers

1. HyperText Markup Language
2. The primary purpose of HTML is to tell browsers how to structure webpages that users visit
3. AN element may contain a data item or a chunk of text or image
4. HTML attributes are special words placed inside an element's opening tag, to provide additional information or configuration to that element
5. \<!doctype html> tells the browser that the document is using modern HTML
6. \<head> contains information about the webpage that isn't part of the content that users see.
7. \<body> it contains the actual content that user see on the webpage
8. \<h1> is used for the main heading and \<h2> is used for subheadings
9. \<ul> are used to markup list of items for which the order of the item does not matter while \<ol> are list in which the order of the list of item does matter
10. \<a> anchor encloses text and turn them into links
11. href is an attribute that specifies the URL the link points to
12. alt gives a description of the image, especially for user who can't view the image
13. we would use \<header>, \<main>, \<section> and \<footer> to add semantic structure to the webpage
14. HTML creates the literal components of the page. It tells the browser what content to display. It also define structures, embeds media and creates inputs like text and hyperlinks. CSS manages the aesthetics. It tells the browser how the HTML elements should look. It controls the layout i.e placements, margins etc, also applies visual and modifies the website to look perfect on mobile, tablet and desktop. Javascript handles the behavior. It is a programing language that makes static pages think and act