Knowledge check answers

1. \<nav> wraps a block of links meant for navigating the website
   
2. \<section> is used to group related content together into chapter within a page and \<article> is used as an independent content that makes sense on its own.
   
3. \<aside> is used to hold content that is indirectly related to the main content around it e.g sidebars
   
4. It creates an link for contact. clicking it jumps the user to a specific section i.e contact
   
5. It gives that specific section a unique name on the webpage.
   
6. Form is used to collect user input. It creates containers for interactive controls like text fields, buttons, checkboxes
   
7. clicking on \<label> automatically focuses its connected input field. it tells the screen readers exactly what a specific input field is for.
   
8.  They are linked together. The for attribute matches the id attribute. clicking the label automatically focuses the input field
   
9.  They define different data constraints. type="text" accepts alphanumeric text while type="email" requires a valid email address
    
10. Required forces users to fill out the field. it prevent form submission if left blank
    
11. They create a drop down selection list. \<select> defines the outer drop-down menu \<option> defines individual choices within that menu
    
12. \<textarea> is used for multi-line text input e.g. comments while input is used for single-line text input
    
13. the purpose of buttons is to t submit the form data to a server. HTML creates the form. JavaScript can control its behavior. Backend code can process the submitted data. 
    
14. Semantic HTML is important because it helps screen readers interpret web page layout and content properly for visually impaired users and also makes the source code easier for developers to read, understand and update
    
15. The form will include: contact info i.e
Client information
    Name
    Email
    Phone
    Company/Organization
Project information
    Service required
    Quantity
    Preferred size
    Deadline
Additional information
    Message / Project description